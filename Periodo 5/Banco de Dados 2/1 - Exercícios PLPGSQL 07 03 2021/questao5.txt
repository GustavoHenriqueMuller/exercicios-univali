CREATE TABLE aluno (
  id SERIAL PRIMARY KEY,
  nome VARCHAR,
  nota INT
);

CREATE OR REPLACE FUNCTION tresMelhoresAlunos() RETURNS SETOF RECORD AS
$$
	SELECT (nome, nota) FROM aluno ORDER BY nota DESC LIMIT 3;
$$
LANGUAGE SQL;

SELECT tresMelhoresAlunos();
